# Hyprlock Config - Catppuccin Frappe

$base = rgb(303446)
$surface = rgb(414559)
$text = rgb(c6d0f5)
$subtext = rgb(b5bfe2)
$accent = rgb(8caaee)
$error = rgb(e78284)

general {
    hide_cursor = true
    grace = 0
}

background {
    monitor =
    path = screenshot
    blur_passes = 3
    blur_size = 8
    noise = 0.02
    contrast = 0.9
    brightness = 0.8
}

input-field {
    monitor =
    size = 300, 60
    outline_thickness = 3
    dots_size = 0.25
    dots_spacing = 0.15
    dots_center = true
    outer_color = $accent
    inner_color = $base
    font_color = $text
    fail_color = $error
    fade_on_empty = false
    placeholder_text = <i>Password...</i>
    hide_input = false
    position = 0, -20
    halign = center
    valign = center
}

label {
    monitor =
    text = $TIME
    color = $text
    font_size = 96
    font_family = FiraCode Nerd Font
    position = 0, 180
    halign = center
    valign = center
}

label {
    monitor =
    text = Hi, $USER
    color = $subtext
    font_size = 28
    font_family = FiraCode Nerd Font
    position = 0, 70
    halign = center
    valign = center
}
